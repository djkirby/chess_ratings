<%= form_for(@chess_game) do |f| %>
  <% if @chess_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chess_game.errors.count, "error") %> prohibited this chess_game from being saved:</h2>

      <ul>
      <% @chess_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :white_player_id %><br>
    <%= f.number_field :white_player_id %>
  </div>
  <div class="field">
    <%= f.label :black_player_id %><br>
    <%= f.number_field :black_player_id %>
  </div>
  <div class="field">
    <%= f.label :winner_id %><br>
    <%= f.number_field :winner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
